services:
  my-service-web:
    # ECR에 이미 빌드되어 있는 latest 이미지 사용
    image: 925047940866.dkr.ecr.ap-northeast-1.amazonaws.com/std01-nginx:latest

    # Build는 GitHub Actions에서 수행
    # build:
    #   context: ./nginx
    #   dockerfile: Dockerfile

    container_name: nginx-container

    ports:
      - "80:80"

    # volumes:
    #   - /home/ec2-user/nginx/conf.d/default.conf:/etc/nginx/conf.d/default.conf:ro
    #   - /home/ec2-user/nginx/html:/usr/share/nginx/html:ro
    #   - /home/ec2-user/nginx/logs:/var/log/nginx

    restart: always

    # networks:
    #   - public-subnet
    #     aliases:
    #       - nginx

# networks:
#   public-subnet:
#     driver: bridge